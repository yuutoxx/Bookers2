<%= render 'books/user', user: @user, book: @book %>
<div class="col-md-8 offset-md-1">
  <h2>Users</h2>
  <table class="table">
    <thead>
      <tr>
        <th>image</th>
        <th>name</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <thead>
      <%= @user.each do |user| %>
        <tr>
          <
        </tr>
      <% end %>
    </thead>
  </table>
</div>